 Library API — Book Management (DRF + JWT)

A Django REST API for managing a library book collection with secure CRUD operations protected by  Authentication.

✨ Features

CRUD for books

Tokenauthentication

DRF ModelViewSet architecture

Clean, well-structured API
